<div>
  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %> fade in">
      <a href="#" data-dismiss="alert" class="close black_color">×</a>
      <%= value %>
    </div>
  <% end %>
</div>
